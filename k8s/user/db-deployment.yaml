apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-db
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-db
  template:
    metadata:
      labels:
        app: user-db
    spec:
      containers:
        - name: mongo
          image: mongo:7.0
          ports:
            - containerPort: 27017
          volumeMounts:
            - mountPath: /data/db
              name: user-db-data
      volumes:
        - name: user-db-data
          persistentVolumeClaim:
            claimName: user-db-pvc