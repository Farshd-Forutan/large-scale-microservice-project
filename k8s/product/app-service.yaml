apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: microservices
spec:
  type: LoadBalancer
  selector:
    app: product-service
  ports:
    - port: 5001
      targetPort: 5001